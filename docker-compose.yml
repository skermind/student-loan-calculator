services:
  backend:
    build: ./backend
    container_name: loan-backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: loan-frontend
    ports:
      - "3001:3000"
