services:
  backend:
    build: ./backend
    container_name: loan-backend
    expose:
      - "8000"

  frontend:
    build: ./frontend
    container_name: loan-frontend
    expose:
      - "3000"

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - frontend
      - backend